---
name: Session May 21 2026 PM
description: System architecture folder, MASTER.md, CLAUDE.md restructure, browser Copy/Clear All, CADE-PROFILE rebuild
type: project
originSessionId: c1b4fc12-8eb4-43d8-aa1a-fe26c1bdfc7a
---
## (1) Date and Time
May 21, 2026 — approximately 6:30 PM – 8:51 PM

## (2) Topics Discussed

1. **Drive layout in to-do list** — drive layout doesn't belong on a to-do list; belongs in memory as a reference
2. **system_architecture/ folder** — created as a memory subfolder to hold hardware and software reference files permanently
3. **Permission rule enforcement** — extensive discussion on why Claude violated the "one yes = one action" rule; root cause traced to MEMORY.md not being actively processed even when in context
4. **MASTER.md** — designed and built a startup reading chain: one SessionStart hook → reads MASTER.md → five ordered "Cade orders: Read X now." commands; elegant solution to force active reading at session start
5. **SessionStart hook consolidation** — collapsed to single command pointing at MASTER.md (instead of multiple separate read commands)
6. **Every-5-messages hook cleanup** — removed MEMORY.md from user_prompt_hook.py injection since CLAUDE.md item 1 already issues "Cade orders: Read MEMORY.md now." every 5 messages
7. **Browser Copy All / Clear All buttons** — QToolBar with QPushButton; Copy All uses page().toPlainText() to capture full page regardless of scroll; Clear All loads EDIT_TEMPLATE editable paste area
8. **Browser as LLM pass-through** — paste LLM responses into browser → Copy All → paste to Claude; enables consensus workflow without screenshot limitations
9. **LLM consensus review** — yesterday's consensus (from ChatGPT/Gemini/Perplexity) reviewed; all architecture items already addressed in current browser.py
10. **CADE-PROFILE.md rebuild** — recreated after file corruption; item 1: name, dog Josh, based in NYC
11. **CLAUDE.md full restructure** — rules reordered by priority (startup → response behavior → permissions → operational → session end); system/reference info moved to architecture files; raw number gaps fixed via full rewrite
12. **Rule 2 moved from CLAUDE.md to MASTER.md** — "At the start of every new chat — load the most recent session file from MEMORY.md" is startup-only; CLAUDE.md re-injects every 5 messages; moved so it fires once via hook

## (3) Files Created or Changed

**Created:**
- `/home/cade/MASTER.md` — startup reading chain (6 ordered reads)
- `/home/cade/.claude/projects/-home-cade/memory/CADE-PROFILE.md` — Cade profile (name, dog, NYC)
- `/home/cade/.claude/projects/-home-cade/memory/system_architecture/reference_system_hardware.md` — drives, GPU, monitors, dual-boot
- `/home/cade/.claude/projects/-home-cade/memory/system_architecture/reference_system_software.md` — OS, Python, venv, daily software, key paths, GitHub

**Changed:**
- `/home/cade/CLAUDE.md` — full restructure; rule 2 removed at end of session; now 10 clean rules
- `/home/cade/.claude/settings.json` — SessionStart hook consolidated to single MASTER.md read command
- `/home/cade/agents/hooks/user_prompt_hook.py` — MEMORY.md removed from every-5 injection
- `/home/cade/agents/browser/browser.py` — Copy All + Clear All buttons added; EDIT_TEMPLATE paste area added
- `/home/cade/.claude/projects/-home-cade/memory/MEMORY.md` — updated with pointers to new architecture files, CADE-PROFILE, session file
- `/home/cade/.claude/projects/-home-cade/memory/project_file_organization.md` — drive layout section removed; renamed to Session To-Do List

## (4) Decisions Made and Why

1. **system_architecture/ folder** — reference data (hardware specs, software versions) belongs in memory as permanent reference, not in CLAUDE.md which injects every 5 messages
2. **MASTER.md chain pattern** — one hook fires → one file → N ordered reads; avoids listing every file in the hook; easy to add/remove reads by editing one file
3. **MEMORY.md removed from every-5 hook** — CLAUDE.md item 1 already handles it; double-injection wastes tokens and is redundant
4. **Copy All via toPlainText()** — captures full page content regardless of scroll position, unlike screenshots which only capture visible area
5. **Rule 2 moved to MASTER.md** — startup-only instructions don't belong in CLAUDE.md because CLAUDE.md re-injects every 5 messages; MASTER.md fires once at session start
6. **CLAUDE.md rule ordering** — priority order: startup commands → response behavior → permissions → operational → session end; makes the most critical rules fire first on re-injection

## (5) What Was Left Unfinished

1. **KP6 hotkey flakiness** — noticed, explicitly deferred
2. **Browser default: wallpaper vs EDIT_TEMPLATE** — wallpaper condition in load_default() fires before EDIT_TEMPLATE fallback; browser opens with wallpaper when no file specified; workaround is clicking Clear All; full fix deferred
3. **Handshake ping for browser crash recovery** — LLM consensus item (edge case 3); deferred
4. **LLM consensus on current browser.py** — browser paste workflow is now set up for this; not yet done
5. **Sensor agent build** — still top priority on to-do list; not started

## (6) What to Pick Up Next Time

1. Run LLM consensus on current browser.py using the new paste workflow
2. Fix browser default — make EDIT_TEMPLATE the default when no file is specified (wallpaper only when explicitly requested)
3. Start sensor agent build
4. Investigate KP6 flakiness when it becomes a real problem
